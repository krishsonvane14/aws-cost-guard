{
  "name": "aws-cost-guard",
  "version": "1.0.0",
  "description": "Monitor and alert on AWS costs using Cost Explorer API and webhooks",
  "type": "module",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsx src/index.ts",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@aws-sdk/client-cost-explorer": "^3.758.0",
    "dotenv": "^16.4.7"
  },
  "devDependencies": {
    "@types/node": "^20.17.19",
    "tsx": "^4.19.2",
    "typescript": "^5.8.2"
  }
}
